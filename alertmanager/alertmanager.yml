global:
  resolve_timeout: 5m

route:
  receiver: 'console'
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: 'console'
    webhook_configs:
      - url: 'http://localhost:5001/'   # you can use a debug webhook or local testing app

# Example Slack receiver (uncomment and configure):
#  - name: 'slack-notifications'
#    slack_configs:
#      - api_url: 'https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXX'
#        channel: '#alerts'
